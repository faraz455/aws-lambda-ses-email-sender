{
  "name": "aws-lambda-ses-email-sender",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "dotenv": "^16.3.1",
    "validator": "^13.15.15"
  },
  "description": "A **production-grade AWS Lambda function** to send emails via **Amazon SES (Simple Email Service)**. Designed for clarity, security, and simplicity, this Lambda function receives email details (recipient, subject, message) as input and sends emails using `aws-sdk`.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
